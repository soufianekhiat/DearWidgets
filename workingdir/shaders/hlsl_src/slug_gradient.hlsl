// slug_gradient.hlsl — COLR v1 linear gradient permutation of the Slug GPU font shader.
// Uses an extended vertex format with per-glyph gradient parameters.
// The pixel shader computes gradient color from the em-space coordinate.
#define SLUG_GRADIENT
#include "slug.hlsl"
